[project]
name = "plamp"
dynamic = ["version"]
description = "Host tools and firmware for plamp."
requires-python = ">=3.11"
dependencies = [
  "fastapi",
  "Pillow",
  "pyserial",
  "pyudev",
  "uvicorn[standard]",
]

[tool.uv]
package = false
